1 - CREATE TABLE employee (
	id INTEGER NOT NULL,
	name VARCHAR(50),
	birthday DATE,
	email VARCHAR(100)
);
3- UPDATE employee
SET name = 'DENEME'
WHERE name LIKE ('K%')
RETURNING*;
4- DELETE FROM employee
WHERE id = 1
RETURNING*;
